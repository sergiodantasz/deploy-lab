server {
    listen 80;
    listen [::]:80;

    server_name ${DOMAIN};

    server_tokens off;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        try_files $uri =404;
    }

    location / {
        add_header Retry-After 120 always;
        return 503;
    }
}
